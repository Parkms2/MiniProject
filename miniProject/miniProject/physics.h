#pragma once
#include"header.h"
#include"game.h"


class Physics {
public:
	/*
	double SdlDeltaTime() {

		Uint64 last = 0, deltaTime = 0;
		Uint64 now = SDL_GetTicks();
		deltaTime = now - last;
		last = now;
		return deltaTime;
	}*/
};